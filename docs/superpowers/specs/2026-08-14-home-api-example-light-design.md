# 首页 API 示例浅色卡片设计

## 目标

`data-home-api-example` 始终显示为浅色代码示例卡片，不跟随系统或页面深色主题。

## 样式边界

- 卡片使用稳定的浅色背景、深色正文和低对比浅灰边框。
- 语言切换器的选中态使用炭灰底与白字，未选中态使用浅灰底与灰色文字。
- 代码、底部说明和 API Key 操作入口均使用浅色卡片内可读的灰阶颜色。
- 只调整该卡片的局部类名，不添加主题判断，不修改全站颜色变量。

## 验证

组件契约测试断言卡片不再包含深色背景与白色正文类，并保留浅色背景、深色文字、示例内容和链接。
