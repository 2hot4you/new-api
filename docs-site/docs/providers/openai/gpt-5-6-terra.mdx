---
title: "GPT 5.6 Terra"
sidebar_position: 9
---

# GPT 5.6 Terra

OpenAI GPT-5.6 中端平衡版，日常生产性价比高。

- **模型 ID：** `gpt-5.6-terra`
- **Provider：** OpenAI
- **输入模态：** text、image
- **输出模态：** text
- **英文简介：** OpenAI GPT-5.6 balanced mid-tier for everyday production work.
- **发布日期：** 2026-07-09
- **知识截止：** 2026-02
- **上下文 Token 上限：** 1050000
- **最大输出 Token：** 128000

## 能力

- function\_calling
- streaming
- vision
- json\_mode
- structured\_output
- reasoning
- tools
- system\_prompt
- web\_search
- caching

## 支持参数

- stream
- temperature
- top\_p
- max\_tokens
- tools
- tool\_choice
- reasoning\_effort
- response\_format

## 兼容协议与安全调用

使用环境变量 `MOLII_API_KEY`，不要把密钥写入代码或客户端。

### OpenAI Chat Completions

**[POST /v1/chat/completions](/api-reference/chat-completions)**

```bash
: "${MOLII_API_BASE_URL:?Set MOLII_API_BASE_URL first}"
: "${MOLII_API_KEY:?Set MOLII_API_KEY first}"

curl --fail-with-body --silent --show-error \
  --max-time 60 \
  --request POST \
  --url "${MOLII_API_BASE_URL%/}/v1/chat/completions" \
  --header "Authorization: Bearer $MOLII_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{"model":"gpt-5.6-terra","messages":[{"role":"user","content":"你好"}]}'
```

## 调用提示

同步接口在返回响应后完成。异步接口请保留任务 ID 并轮询结果；网络超时不表示请求未受理，提交前先查询任务，避免重复付费请求。

通用接口参考：[模型列表](/api-reference/models)、[认证](/api-basics/authentication)。
