---
title: "Gemini 3.6 Flash"
sidebar_position: 12
---

# Gemini 3.6 Flash

Google 前一代 Flash，平衡速度与多模态。

- **模型 ID：** `gemini-3.6-flash`
- **Provider：** Google
- **输入模态：** text、image、audio、video、file
- **输出模态：** text
- **英文简介：** Previous Google Flash model balancing speed and multimodal performance.
- **发布日期：** 2026-07-21
- **知识截止：** 2026-07
- **上下文 Token 上限：** 1048576
- **最大输出 Token：** 65536

## 能力

- function\_calling
- streaming
- vision
- json\_mode
- structured\_output
- reasoning
- tools
- system\_prompt
- web\_search
- code\_interpreter
- caching

## 支持参数

- stream
- temperature
- top\_p
- max\_tokens
- tools
- tool\_choice
- reasoning\_effort
- response\_format

## 兼容协议与安全调用

使用环境变量 `MOLII_API_KEY`，不要把密钥写入代码或客户端。

### Gemini GenerateContent

**POST /v1beta/models/gemini-3.6-flash:generateContent**

```bash
curl --fail-with-body --silent --show-error \
  --max-time 60 \
  --request POST \
  --url "${MOLII_BASE_URL:-https://api.molii.co}/v1beta/models/gemini-3.6-flash:generateContent" \
  --header "x-goog-api-key: $MOLII_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{"contents":[{"parts":[{"text":"你好"}]}]}'
```

### OpenAI Chat Completions

**POST /v1/chat/completions**

```bash
curl --fail-with-body --silent --show-error \
  --max-time 60 \
  --request POST \
  --url "${MOLII_BASE_URL:-https://api.molii.co}/v1/chat/completions" \
  --header "Authorization: Bearer $MOLII_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{"model":"gemini-3.6-flash","messages":[{"role":"user","content":"你好"}]}'
```

## 调用提示

同步接口在返回响应后完成。异步接口请保留任务 ID 并轮询结果；网络超时不表示请求未受理，提交前先查询任务，避免重复付费请求。

通用接口参考：[模型列表](/api-reference/models)、[认证](/api-basics/authentication)。
