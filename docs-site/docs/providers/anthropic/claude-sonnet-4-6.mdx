---
title: "Claude Sonnet 4.6"
sidebar_position: 6
---

# Claude Sonnet 4.6

Anthropic 前一代 Sonnet，仍广泛可用的平衡模型。

- **模型 ID：** `claude-sonnet-4-6`
- **Provider：** Anthropic
- **输入模态：** text、image
- **输出模态：** text
- **英文简介：** Previous Anthropic Sonnet generation\, still widely used as a balanced model.
- **发布日期：** 2026-02-17
- **知识截止：** 2026-01
- **上下文 Token 上限：** 1000000
- **最大输出 Token：** 64000

## 能力

- function\_calling
- streaming
- vision
- json\_mode
- structured\_output
- reasoning
- tools
- system\_prompt
- web\_search
- caching

## 支持参数

- stream
- temperature
- top\_p
- max\_tokens
- tools
- tool\_choice
- reasoning\_effort
- response\_format

## 兼容协议与安全调用

使用环境变量 `MOLII_API_KEY`，不要把密钥写入代码或客户端。

### Anthropic Messages

**POST /v1/messages**

```bash
curl --fail-with-body --silent --show-error \
  --max-time 60 \
  --request POST \
  --url "${MOLII_BASE_URL:-https://api.molii.co}/v1/messages" \
  --header "x-api-key: $MOLII_API_KEY" \
  --header "anthropic-version: 2023-06-01" \
  --header "Content-Type: application/json" \
  --data '{"model":"claude-sonnet-4-6","max_tokens":256,"messages":[{"role":"user","content":"你好"}]}'
```

### OpenAI Chat Completions

**POST /v1/chat/completions**

```bash
curl --fail-with-body --silent --show-error \
  --max-time 60 \
  --request POST \
  --url "${MOLII_BASE_URL:-https://api.molii.co}/v1/chat/completions" \
  --header "Authorization: Bearer $MOLII_API_KEY" \
  --header "Content-Type: application/json" \
  --data '{"model":"claude-sonnet-4-6","messages":[{"role":"user","content":"你好"}]}'
```

## 调用提示

同步接口在返回响应后完成。异步接口请保留任务 ID 并轮询结果；网络超时不表示请求未受理，提交前先查询任务，避免重复付费请求。

通用接口参考：[模型列表](/api-reference/models)、[认证](/api-basics/authentication)。
