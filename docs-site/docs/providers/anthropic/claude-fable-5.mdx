---
title: Claude Fable 5
sidebar_position: 1
---

# Claude Fable 5

Anthropic 公开可用的 Mythos 级最强模型，面向最苛刻推理与长程 Agent。

- **模型 ID：** `claude-fable-5`
- **Provider：** Anthropic
- **输入模态：** text、image
- **输出模态：** text
- **上下文长度：** 1000000
- **最大输出 Token：** 128000

## 能力

- function\_calling
- streaming
- vision
- json\_mode
- structured\_output
- reasoning
- tools
- system\_prompt
- web\_search
- caching

## 支持参数

- stream
- temperature
- top\_p
- max\_tokens
- tools
- tool\_choice
- reasoning\_effort
- response\_format

## 兼容协议与安全调用

使用环境变量 `MOLII_API_KEY`，不要把密钥写入代码或客户端。### Anthropic Messages

**POST /v1/messages**

```bash
curl -X POST "${MOLII_BASE_URL:-https://api.molii.co}/v1/messages" \
  -H "Authorization: Bearer $MOLII_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"claude-fable-5","max_tokens":256,"messages":[{"role":"user","content":"你好"}]}'
```

### OpenAI Chat Completions

**POST /v1/chat/completions**

```bash
curl -X POST "${MOLII_BASE_URL:-https://api.molii.co}/v1/chat/completions" \
  -H "Authorization: Bearer $MOLII_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"claude-fable-5","messages":[{"role":"user","content":"你好"}]}'
```

## 调用提示

同步接口在返回响应后完成。异步接口请保留任务 ID 并轮询结果；网络超时不表示请求未受理，提交前先查询任务，避免重复付费请求。

通用接口参考：[模型列表](/api-reference/models)、[认证](/api-basics/authentication)。
