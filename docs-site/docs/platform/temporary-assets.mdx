---
title: 临时素材
description: 在个人空间创建、查看、引用和清理临时媒体素材。
audience: user
sidebar_position: 5
---

# 临时素材

在 `/temporary-assets` 管理当前账户的图片、视频和音频临时素材。素材仅用于短期处理；不要把它当作永久文件库。

## 创建和查看

页面会显示可用的创建方式。若出现上传区域，可上传允许类型和大小的媒体；若出现来源地址输入项，可填写可访问的媒体 URL。创建后在列表中查看预览、类型、状态和到期信息，并用类型、ID 或时间范围筛选。

处理中素材尚不能稳定引用。处理成功后再复制或使用页面提供的素材引用；来源不可访问、格式不支持或处理失败时，修正来源后创建新素材。接近过期的素材应及时替换。

## 更新来源和删除

对可编辑的素材可更新来源地址；确认内容无误后保存。删除单个或选中的素材前确认没有任务仍在引用它。删除或到期后，原有临时引用不能用于新的请求。

> 上传和来源地址功能以页面为准。若未出现创建控件，表示该方式当前未启用；不要绕过页面限制上传敏感或无权使用的媒体。

## 状态说明

| 状态 | 处理方式 |
| --- | --- |
| 成功 | 素材处理完成并可预览后，再用于允许的工作流。 |
| 空状态 | 没有素材时，使用当前页面提供的创建方式添加一个。 |
| 未启用 | 上传、来源地址或批量操作未显示时，该功能当前不可用。 |
| 已过期 | 已过期素材不可继续引用；使用原始媒体重新创建。 |
| 错误 | 预览或处理错误时检查格式、大小、来源可访问性，然后新建或重试。 |

## 截图占位

后续可补一张“临时素材列表和创建区域”的普通用户截图，alt：`Molii 临时素材页面，示例素材和来源地址均为合成内容`。暂不提交截图；截图必须使用脱敏或合成的媒体、URL 和账号信息，只展示本人可见的导航项目。
