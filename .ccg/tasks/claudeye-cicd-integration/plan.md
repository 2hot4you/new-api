# Claudeye CI/CD integration
Scope: application deployment infrastructure only. No bluegreen, no live deploy, no main merge. Keep main push existing two production environments until acceptance.
1. Workflow target selector and immutable source resolver: explicit manual targets, development only develop workflow; production only main workflow and exact main event SHA (source_ref must empty or same full SHA); all-production initial existing two only clearly labelled. verify/release checkout same resolved SHA. Resolve ref once; invalid target/ref fails before secrets. New sites manual-only. Existing environment branch policy remains main.
2. Application deploy script adds fixed new environment maps, root /opt/claudeye/production, localhost3000, immutable digest for new sites, independent projects and infrastructure external internal backend plus egress network. Never remove-orphans or down volumes for app, preserve retry/lock/health/rollback. Remove raw failure logs from Actions, store diagnostics privately where needed. Explicit rollback outcome report.
3. Independent generic claudeye brand profile without new business functionality. Existing environment variables configure assets/title; do not invent user branding files. Examples/document all required vars. Missing brand assets remain user input before release.
4. Extend behavioral target and deploy tests, ShellCheck, workflow validation, frontend typecheck/build, Go tests. Run dual external review (record missing tool if unavailable); no false completion claims.
5. Commit feature branch locally and present real blockers/server readiness. Do not push/merge or trigger workflows until implementation/tests complete. Main merge requires explicit production authorization because existing main push deploys aigc.
Ownership: worker workflow owns deploy.yml, target resolver+tests; worker deployment owns deploy.sh/new local DB Compose and deploy shell tests. Lead owns branding/config examples/documentation/integration validation. All agents no further spawning.
