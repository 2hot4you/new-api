# 分析

- 复杂度：M；风险：中。后端仅增加响应字段，前端消费精确样本数并新增可复制入口。
- 性能成功率必须使用原始成功数/请求数加权，不能继续对模型百分比做简单平均。
- API 路由复用状态接口与现有复制组件，保持后台配置顺序；空列表不占空间。
- 按规范尝试并行调用 antigravity 与 Claude 分析，但本机不存在 `~/.claude/bin/codeagent-wrapper`，两路均返回 `no such file or directory`。后续使用仓库代码、测试和本地审查完成验证。
