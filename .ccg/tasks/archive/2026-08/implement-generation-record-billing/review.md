# 实施结果

- 视频生成记录新增公开安全的 `billing` 摘要，不暴露 `TaskPrivateData`、上游任务 ID、密钥或媒体地址。
- 处理中只显示“待结算”，不把预扣额度作为最终费用返回；失败任务区分已退款与退款待处理。
- 成功任务展示最终费用，并支持展开 Seedance 或 Grok Video 的请求参数、单价、分组倍率和公式。
- Grok 明细使用最终时长、最终分辨率与素材输入费用；不展示预估时长、预估分辨率、尺寸或帧率。
- 历史任务缺少完整快照时只展示最终费用，并提示历史明细不可用。
- 图像生成记录继续复用现有 Grok Image 使用日志计费展示，未重复实现。

# 验证

- `go test ./...` 通过。
- `npm run build:check` 通过。
- 本次文件定向 `oxlint` 通过。
- `npm run format:check` 通过。
- 生成记录计费前端测试 3 项通过。
- 全量前端 lint 仍被仓库既有、与本次无关的历史规则错误阻断。
- 本机常驻服务已重新构建并通过 `com.molii.new-api` 重启，`GET /api/status` 正常。
