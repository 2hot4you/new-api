# 炭灰系统主色审查

## 结论

通过。系统基础品牌交互色已从蓝色调整为中性炭灰，主题切换入口和切换行为保持关闭。

## 需求核对

- 亮色主色为深炭灰 `oklch(0.28 0 0)`，搭配近白前景色。
- 暗色主色为较亮中性灰 `oklch(0.82 0 0)`，搭配深色前景色。
- 焦点环、侧栏主色、侧栏焦点环及相关前景色均去除蓝色色相。
- `accent` 和 `sidebar-accent` 继续从主色混合生成，无额外硬编码主题色。
- Serif 字体、圆角、密度和系统亮暗模式保持不变。
- 成功、警告、信息、错误及图表颜色未灰阶化。
- 公共页面未出现主题切换按钮；应用头部配置抽屉默认仍为关闭，主题 provider 的切换方法仍为空操作。

## 测试与验收

- TDD 红灯：旧蓝色 `--primary` 与预期炭灰不一致，颜色契约测试正确失败。
- TDD 绿灯：炭灰令牌写入后，亮暗模式及语义色契约全部通过。
- 前端全量测试：309 项通过，0 项失败。
- TypeScript 类型检查通过。
- 格式检查通过，检查 1150 个文件。
- 新增测试文件的 lint 通过。
- `git diff --check` 通过。
- 本地服务健康：`127.0.0.1:3000/api/status`、`127.0.0.1:3000/pricing`、`127.0.0.1:3001/pricing` 均返回 200。
- 本地浏览器亮色验收：页面使用 Serif；主交互及选中态为无色相中性灰；未发现主题切换入口。
- 暗色令牌由真实 CSS 通过 Happy DOM 计算验证；亮暗主色文字对比度约为 14.0:1 和 10.4:1，均高于 WCAG AA 正文要求。

## 说明

- 按用户要求未恢复主题切换功能。
- 按用户要求未执行生产构建。
- 用户此前明确要求不调用 antigravity 或 Claude；本次未启动外部模型或子代理，由主代理完成审查。
