# 审计结论

- `*:3000` 是 Go 后端，也是浏览器唯一需要访问的统一入口。
- `127.0.0.1:3001` 是 Rsbuild 前端开发服务，仅用于热更新。
- Go 后端通过 `FRONTEND_DEV_SERVER_URL=http://127.0.0.1:3001` 将页面和热更新流量代理到 3001。
- 3001 只绑定回环地址，不对局域网或公网开放。
- 在“不构建前端”和“保留实时热更新”的前提下，不能停止 3001；停止后 3000 的页面请求会返回前端开发服务不可用。
- 未修改运行配置或停止任何服务。
