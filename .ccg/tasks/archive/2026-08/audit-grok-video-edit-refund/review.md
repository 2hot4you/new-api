# 审计结论

## 对应任务

- 公开任务：`task_rR90qPDjBcnNcJP7cOGvcu2NhADWecJw`
- 状态：`SUCCESS/100%`
- 操作：`video_edit`
- 模型：`grok-imagine-video`

## 计费核对

- 视频编辑接口不接受调用方指定输出时长和分辨率。
- 提交时采用保守上限估算：8.7 秒、720p。
- 预估费用：`8.7 × (¥0.07 输出/秒 + ¥0.01 视频输入/秒) = ¥0.696`。
- `QuotaPerUnit = 500000`，因此预扣配额为 `0.696 × 500000 = 348000`。
- 上游终态重新查询返回：时长 6 秒、`cost_in_usd_ticks = 3600000000`，即 1:1 人民币目录价 `¥0.36`。
- 实际费率为 `¥0.36 ÷ 6 = ¥0.06/秒`，对应 480p 输出 `¥0.05/秒` 加视频输入 `¥0.01/秒`。
- 实际配额：`¥0.36 × 500000 = 180000`。
- 差额退款：`348000 - 180000 = 168000`，对应 `¥0.336`。

## 判断

- 这是成功任务的正常差额退还，不是失败退款。
- 视频输入费已包含在实际 `¥0.06/秒` 中，没有漏算。
- 结算测试覆盖实际时长、分辨率档位和多次轮询不重复结算，均通过。
- UI 中“异步任务退款”是通用日志类型；该条记录的实际原因是 `adaptor计费调整`。
- 用户明确要求不调用 antigravity 或 Claude，因此未执行外部双模型审查。
