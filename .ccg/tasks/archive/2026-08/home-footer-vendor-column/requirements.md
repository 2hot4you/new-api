# 首页 Footer 厂商列

- 默认首页 Footer 列顺序为产品、开发者、厂商、支持。
- 厂商数据复用首页已有 `/api/pricing` catalog，不新增请求或硬编码列表。
- 供应商顺序遵循后台 `display_order`，与模型广场一致。
- 只展示模型广场内有可用模型的供应商。
- 每项展示供应商配置图标和名称，并链接到对应 `/pricing?vendor=...` 筛选。
- 展示全部供应商；内容较多时 Footer 自然增高，上下边距和列间距保持一致。
- 移动端继续使用两列布局。
- 自定义 Footer HTML 路径保持不变。
