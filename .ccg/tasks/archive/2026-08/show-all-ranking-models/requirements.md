# 排行榜完整模型展示

## 目标

- 热门模型历史图为每个有用量的模型 ID 保留独立序列。
- 不再把第 11 名及之后的模型合并为 `Others`。
- 汇总 Tooltip 不限制前 10 行，不显示“还有 N 项”，展示全部模型。
- 每个模型继续使用 metadata 中配置的 model icon。
- `总计`保留在首行，模型行按当前桶 Token 用量降序排列。
- 内容超过视口时允许 Tooltip 内容区域滚动，但不删除或合并模型行。

## 边界

- 供应商市场份额的 `Others` 聚合保持不变。
- 不修改排行榜时间范围、Token 统计、模型 metadata 或计费。
- 不修改 GPT Image 2 渠道。
