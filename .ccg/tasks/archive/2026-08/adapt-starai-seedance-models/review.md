# 审查结果

## 范围核对

- 新增 Seedance 2.0 Mini 与 Seedance 2.5 模型注册、能力边界和模型广场元数据。
- 新增 Mini 与 2.5 的公开刊例价矩阵及管理员配置项，没有加入采购折扣或测试密钥。
- 成功任务仅在 URL 为 HTTPS、主机严格匹配火山 TOS 且包含签名参数时直接返回原始地址。
- 查询解析保留真实 usage、duration 和 resolution；最终结算继续使用真实 total_tokens。
- 临时素材限制同步为视频 50 MB，并移除新版上游不支持的 HEIC/HEIF。
- 文档、OpenAPI、前端国际化和回归测试同步更新。

## 安全与兼容性

- 未放宽任意域名访问；TOS URL 校验仍限定精确主机和签名参数。
- 未在公开任务数据中存储或输出测试密钥。
- 未公开或应用上游采购折扣。
- 标准版与 Fast 版既有模型能力和定价保持不变。

## 验证

- `go test ./...`：通过。
- 前端新增及受影响测试：9/9 通过。
- `bun run typecheck`：通过。
- 本次前端文件 scoped oxlint：通过。
- `bun run i18n:check`：通过。
- 前端生产构建：通过。
- 文档契约测试：18/18 通过。
- OpenAPI lint：通过。
- Docusaurus 生产构建：通过。
- `git diff --check`：通过。

## 审查说明

仓库要求的外部双模型审查命令在当前环境不可用（缺少 `codeagent-wrapper`），因此未伪造外部审查结果；本次完成了逐文件人工审查和上述自动化验证。
