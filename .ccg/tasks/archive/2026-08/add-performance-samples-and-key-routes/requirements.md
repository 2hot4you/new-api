# 需求

1. `/dashboard/overview` 的性能健康保留现有 24 小时口径，整体成功率展示成功请求数/总请求数，每个热门模型展示该模型的请求样本量。
2. 后端性能汇总接口以新增字段返回每个模型的 `request_count` 和 `success_count`，不破坏现有字段。
3. `/keys` 的“API 密钥”标题旁展示后台已配置的 API 接入路由名称与 URL，保持配置顺序；点击整个条目复制 URL，并给出复制成功反馈。
4. 未配置 API 路由时不展示占位内容；移动端不能挤压主要操作按钮。
5. 不处理概览页的其他优化项。
