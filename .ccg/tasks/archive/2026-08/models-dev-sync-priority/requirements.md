# Requirements

- 在手动 models.dev 同步弹窗中提供“本地优先”和“models.dev 优先”两种模式。
- 默认选择“本地优先”，保持旧客户端和旧操作的安全行为。
- 本地优先只补充空白模型元数据。
- models.dev 优先覆盖 models.dev 能提供的全部模型元数据。
- 两种模式均不得修改 Molii 本地价格、渠道、模型启用状态或路由配置。
- 同步结果展示新增、更新和跳过数量。
- 后端必须验证同步模式，拒绝未知值。
- 变更需要后端与前端测试，并在本地开发环境验证。
