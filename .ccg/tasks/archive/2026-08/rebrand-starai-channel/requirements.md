# Requirements

- 渠道管理中类型 61 的显示名称由 `StarAI` 改为 `Molii AIGC`。
- 清理管理端、日志、素材上传、计费设置、接口错误消息和交付文档中的用户可见 `StarAI` 品牌字样。
- 保留渠道类型数值 61、Go/TypeScript 内部常量、适配器目录、数据库/Redis key、设置键和兼容错误码，避免破坏已有数据、配置和 API 客户端。
- 不改变上游请求协议、路由、模型 ID、渠道鉴权或计费行为。
- 更新相关前后端测试并验证构建。
