# Grok 渠道 New API 管理接口需求

## 目标

- `Molii Grok Imagine API` 的查询余额与获取模型使用上游 New API 接口。
- 使用用户提供的系统访问令牌与用户 ID 进行只读联调，或按接口能力使用渠道 Key。
- 明确鉴权优先级、失败回退和错误脱敏策略。

## 安全约束

- 系统访问令牌仅用于本次临时联调，不写入源码、配置文件、测试、日志或任务文档。
- 不在命令输出、错误消息或最终交付中回显令牌。
- 只调用余额和模型列表等只读接口，不创建任务、不产生费用。
- 不调用 antigravity 或 Claude。
