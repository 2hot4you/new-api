# 自审结果

## 审查方式

用户明确要求不调用 antigravity 或 Claude，因此本任务未执行 CCG 的外部双模型审查，改为由 Codex 对完整 diff、计费公式、异步结算和安全边界进行直接审查。

## Critical

无未解决问题。

已在审查中修复：

- 视频编辑完成结算最初读取当前配置，可能导致提交后改价影响在途任务；现已把视频输入价及 480p/720p 输出价快照进任务计费上下文。
- 固定价异步任务默认跳过完成结算；现以可选接口只允许 Molii Grok 适配器进入差额结算，其他供应商行为不变，并增加回归测试。
- 图片媒体输入费与输出数量不能相乘；现使用 `输出价 × 输出张数 + 输入价 × 输入张数`。

## Warning

无未解决问题。

视频编辑响应不直接返回输出分辨率，代码使用官方上游 `usage.cost_in_usd_ticks / duration` 在 480p 与 720p 官方组合费率间择近识别。若上游省略费用字段，则按官方编辑上限 720p 结算实际时长，避免按最大 8.7 秒持续多收，同时避免低估输出档位。

## Info

- 上游任务 ID、结果 URL 和费用字段不会写入公开任务响应或安全轮询数据。
- `file_id` 与 URL 媒体输入均保留官方对象结构。
- 工具调用只统计成功/完成的输出项；失败、取消、不完整输出不计费。
- Grok 专属工具价仅匹配 `grok-*` 模型，不改变其他供应商默认工具价格。
- 设置页沿用 `molii-aigc-video-pricing` ID，避免已有导航链接失效。

## 验证证据

- `go test ./...`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- 改动前端文件 `oxlint`：通过。
- `gofmt`：已应用到全部改动 Go 文件。
- `git diff --check`：通过。
- 新增内容未发现真实密钥或持久化测试凭据。
