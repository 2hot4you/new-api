# 需求

- 诊断 Grok 视频任务成功后 `/v1/videos/{task_id}/content` 下载为 93 字节不可播放文件的问题。
- 保持全局 SSRF 防护开启，不允许通过任意域名或私网地址取回内容。
- 仅对白名单中的 Grok 官方视频结果域名提供最小范围例外。
- 例外必须要求 HTTPS、禁止用户信息和非 443 自定义端口，并使用精确域名匹配。
- 保留 Range 下载、MP4 响应头和上游隐私脱敏行为。
- 增加安全边界测试与真实任务下载验证。
- 不调用 antigravity 或 Claude，不推送远端。
