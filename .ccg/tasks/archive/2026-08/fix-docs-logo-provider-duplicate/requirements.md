# Requirements

- 保持 Docusaurus Classic 官方 Header/Footer 布局与样式。
- Header 使用主站同款 Molii Wordmark，不再组合旧 `M` 图标与文字标题。
- `Provider 与模型` 父分类直接提供 Docusaurus 官方 generated-index 入口。
- 自动生成的侧栏中不再出现第二个同名 `Provider 与模型` 子页面。
- 各 Provider 介绍页、模型页、搜索、固定浅色和 `/docs/` 子路径部署保持不变。
