# Requirements

- 在分组定价页面连续输入 Icon 或推荐指数时，输入框必须持续保持焦点。
- 不改变分组排序、计费映射、保存入口、推荐指数校验或 API Key 展示语义。
- 增加能够证明输入节点未被重建的回归测试。

# Root-cause hypothesis

展示顺序与旧计费映射的键顺序不一致时，当前签名比较把 JSON 对象插入顺序当成业务差异；元数据字段首次变化后同步 effect 误判并重建所有行，生成新的 `_id`，从而替换输入 DOM 节点并丢失焦点。
