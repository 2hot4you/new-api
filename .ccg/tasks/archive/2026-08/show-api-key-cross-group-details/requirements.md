# 跨分组详情弹框

- `/keys` 表格中，普通单分组继续使用现有静态徽标。
- 跨分组单元格可点击，并在紧凑 Popover 中展示该 API Key 的故障转移顺序。
- 显式保存的 `auto_groups` 是顺序真值；旧记录缺少该字段时使用系统自动分组顺序并明确标注。
- 每项展示序号、图标、分组名、倍率、推荐指数和可用说明。
- 已从当前可选分组中移除但仍存在于令牌配置里的分组必须显示为不可用，不能静默过滤。
- 弹框只允许纵向滚动，支持鼠标和键盘操作，不修改后端路由或执行逻辑。
