# 需求

- 诊断 `POST /v1/images/edits` 对 Grok 图片编辑请求返回 400 的真实原因。
- 保持渠道凭据、上游域名、上游请求 ID 和原始错误体不对用户公开。
- URL 图片输入按官方请求契约补齐 `type: image_url`。
- 将内容审核拒绝映射为稳定、可识别的 Molii 错误码，不再误报为通用服务故障。
- 增加回归测试，重启本地常驻后端并完成真实接口验证。
- 不调用 antigravity 或 Claude，不推送远端。
