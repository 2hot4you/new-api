# 分组推荐指数小数与徽标

- 推荐指数允许 `0.0` 至 `5.0`，最多一位小数。
- 管理端允许逐步输入小数，不因受控状态更新失焦。
- `/keys` 新增/编辑令牌的分组下拉选项和当前选中区域使用现有 Badge 组件展示推荐指数。
- 展示值固定保留一位小数，例如 `5.0`、`3.8`。
- 未配置、无效或为 `0` 时不展示评分徽标。
- `/keys` 表格分组列不展示推荐指数。
- 不改变分组倍率、排序、自动分组与计费行为。
