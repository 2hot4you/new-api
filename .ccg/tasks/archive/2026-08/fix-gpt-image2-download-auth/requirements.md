# 修复 GPT Image 2 预览图片下载授权失败

## 目标

- 已登录用户能够从使用日志详情直接下载本人 GPT Image 2 临时预览图片。
- 下载仍需校验日志归属，不能把 COS 私有对象暴露为公共资源。
- 下载行为触发附件保存，不打开新标签页。
- `/usage-logs/common` 与 `/usage-logs/drawing` 共用的详情组件同时生效。

## 验收标准

- 浏览器下载请求使用与现有 API 客户端一致的登录凭证。
- 后端继续拒绝未认证、跨用户、过期或不存在的资源。
- 有覆盖授权请求和下载行为的回归测试。
