# 需求

- `/keys` 创建和编辑 API 密钥时，将已选择的分组按模型 Provider 分区展示。
- 不同 Provider 之间不展示故障转移箭头或全局优先级，表达为按请求模型独立匹配。
- 只有同一 Provider 下存在多个已选接入点时，才展示排序控制。
- 分组行必须展示图标：优先分组图标，缺失时回退到 Provider 图标。
- 保持现有 `auto_groups` 提交格式与后端选路逻辑不变。
- 保留已有模型限制过滤、成功率、倍率和删除能力。
