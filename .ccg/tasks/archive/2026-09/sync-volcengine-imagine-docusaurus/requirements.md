# 同步 Volcengine Imagine 客户指南到 Docusaurus

- Bytedance Provider 索引必须列出 Seedance 2.0、Fast、Mini、2.5 四个模型。
- 四个模型必须拥有独立 Provider 页面，展示正确能力、分辨率、时长和参数。
- Seedance API 参考和示例统一使用推荐的 `/v1/videos` 创建/查询路径，并保留 `/v1/video/generations` 兼容说明。
- 补齐文生视频、首帧、首尾帧、多模态、编辑、延长、联网搜索、任务查询与结果下载示例。
- 补齐临时素材创建、查询、引用和删除流程。
- 不公开上游域名、测试密钥、内部渠道字段或内部折扣。
- 通过 Docusaurus 文档契约测试和生产构建，提交并推送到当前分支。
