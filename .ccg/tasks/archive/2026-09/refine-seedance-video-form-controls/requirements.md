# Seedance 视频工作台表单修正

- API 密钥选择器的已选值和下拉选项均仅显示脱敏的 `sk-xxxx…xxxx`，不展示数据库 ID。
- 提示词编辑区降低到与右侧预览区域协调的高度，保留纵向调整能力。
- 生成模式的已选值和下拉选项统一显示中文，不展示 `text`、`frames`、`references` 内部枚举。
- 时长改为固定下拉选项，只展示当前模型允许的整数秒数。
- 不修改视频生成 API 契约或后端计费逻辑。
