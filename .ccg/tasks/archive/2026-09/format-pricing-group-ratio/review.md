# 审查结果

## 范围

- 模型广场分组筛选中的倍率标签格式。
- 整数倍率至少保留一位小数，非整数倍率继续按有效精度展示。

## 结论

- Critical：无。
- Warning：无。
- Info：仅修改展示字符串，不修改分组倍率数据或计费计算。

## 验证证据

- RED：修改前组件显示 `x1`，新增断言按预期失败。
- GREEN：组件同时显示 `x1.0` 和 `x0.5`，相关 25 项测试通过。
- 定向 Oxlint、TypeScript 类型检查和生产构建通过。
