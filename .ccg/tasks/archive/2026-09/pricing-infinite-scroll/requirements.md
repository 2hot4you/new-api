# 模型广场无限滚动需求

- `/pricing` 卡片视图与表格视图均取消分页。
- 首批显示 20 个模型，接近页面底部时继续显示下一批。
- 搜索、筛选或排序改变模型集合后，恢复为首批 20 个。
- 卡片与表格切换时保留当前已显示数量。
- 桌面左侧筛选栏固定于视口，主内容滚动不带动筛选栏。
- 鼠标位于左侧筛选栏时允许其独立滚动，并阻止滚动穿透。
- 移动端筛选抽屉保持现有行为。
- 浏览器不支持 `IntersectionObserver` 时直接显示全部结果，不能退回分页。
