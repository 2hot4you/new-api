# 需求

本地、开发和生产环境统一采用宽松的应用层限流策略：正常页面、普通 API 和会话刷新不应因共享限流桶受到阻断；只对登录、注册、验证/找回密码、支付与敏感密钥操作保留高阈值、相互隔离的防滥用保护。

# 验收标准

- 默认关闭全局 Web 和全局 API 限流。
- 会话刷新、登出、普通读取与日志接口不使用关键接口限流。
- 登录、注册、验证与找回密码的限流桶按用途隔离，不能互相消耗额度。
- 已认证敏感操作按用户与用途隔离。
- 429 保留 `Retry-After`。
- 覆盖 Redis 与内存限流路径的回归测试。
