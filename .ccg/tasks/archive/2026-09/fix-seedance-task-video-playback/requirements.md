# Requirements

- `/usage-logs/task` 中成功的 Seedance 视频任务可以直接预览刚生成的视频。
- 下载按钮沿用短期签名 URL，无需暴露或要求用户的 API Key，并返回附件下载响应。
- 无效、篡改或过期签名仍必须被拒绝。
- 不删除 Doubao 插件，除非确认统一视频协议、Ark/VolcEngine 渠道和 Seedance 模型声明均已有完整替代。
- 保持现有 Molii Grok 视频播放与下载行为不变。
