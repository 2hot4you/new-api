# Seedance 视频工作台优化

- 画面比例 `adaptive` 在界面显示为“自动”，请求值保持 `adaptive`。
- 核实并支持智能视频时长；界面显示为“自动”，请求值保持协议值 `-1`。
- 页面刷新后仅自动恢复未完成任务；无未完成任务时显示占位预览。用户主动从历史记录选择已完成任务时仍可预览。
- API 密钥脱敏使用 `****`，不再使用 `xxxx`。
- 按 Seedance 协议区分文生视频、首尾帧、多模态参考三种模式的素材组件与 `@` 引用能力，避免隐式切换和角色混用。
- 自动时长的付费确认不得显示明显偏低或误导性的预估。
