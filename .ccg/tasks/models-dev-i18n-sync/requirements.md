# 当前需求

- 模型元数据继续从 models.dev 同步。
- 不采用 `localhost:3005` 或 ZenMux 作为 New API 数据源。
- 保留自动同步与管理员手动同步能力。
- models.dev 只提供英文简介时，不做运行时自动翻译，由开发者人工英译中并写入 Web i18n。
- 当前启用模型补齐简体中文与繁体中文简介；其他语言缺少人工译文时回退英文。
- 后续新增模型可先同步英文，待人工补充中文翻译后随代码发布。
- 模型广场按当前 Web 语言展示对应简介并提供明确回退规则。
- 模型价格、分组倍率、渠道映射和启用状态不由元数据同步覆盖。
