# 实施约束

- 按已归档计划 `.ccg/tasks/archive/2026-09/evaluate-upstream-rc30-upgrade/plan.md` 执行。
- 从 `v1.0.0-rc.24` 到 `v1.0.0-rc.30` 逐版本整合，每个版本独立验证和提交。
- 不直接修改 `develop`，不推送，除非用户另行要求。
- 不连接或修改实际数据库；数据库变更先以测试、预检和文档形式实现。
- 保留 Molii 的有序跨分组路由、StarAI、Molii Grok、临时素材、COS、GPT Image 2、
  模型定价、用量日志和真实结算行为。
- `rc.28`、`rc.29` 只能作为代码集成检查点，不能作为运行版本。
- 所有行为变更遵循测试先行；任何计费、额度、数据库或任务渠道回归均阻断推进。
